{
  "debug": {
    "type": "boolean",
    "default": true,
    "description": "Enable Trinity debug output in the developer tools console."
  },
  "fileFilters": {
    "type": "string",
    "default": "*.test.js",
    "description": "Filter to match to run Trinity on save. Separate patterns with semicolons."
  },
  "babelOptions": {
    "type": "object",
    "properties": {
      "resolvePath": {
        "type": "string",
        "default": "./src/_local_modules",
        "description": "When Babel encounters an import statement with Resolve Prefix it uses this directory to resolve the module."
      },
      "resolvePrefix": {
        "type": "string",
        "default": "#",
        "description": "When Babel encounters this prefix in your import statements, it replaces it with the Resolve Path."
      }
    }
  }
}
